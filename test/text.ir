::Test

:run
  sendKeys('o')
  sendInput('abc')
  sendKeyCode(DOM_VK_ESCAPE)
  itemlist := select('.itemlist')
  assert(itemlist.children.length == 1)
  assert(itemlist.firstChild.innerText.trim() == 'abc')
  report('done')
