::
  // Taking the parameter as non-nullable, but the member needs to be nullable
  // for disposing.
  el\!Element\$
  @element+\Element\ el

  @bless()

:@bless
  @element.item = @
  @element.setAttribute('active', '1')
  @element.dragger = &drag.Move(@element)
  listen(@element, 'click', ##)
    evt\Event\$
    if @element.getAttribute('active') && !evt.ctrlKey
      f_page.selection.select(@element, evt.shiftKey)

:dispose
  @element.item = null
  @element = null
