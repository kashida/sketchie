::
  @list+\%.List\ &%.List()
  @selection+\%.Selection\ &%.Selection()

:clear
  @list.clear()
  @selection.clear()

Make the dom suitable for saving.
:freeze
  @list.freeze()
  // TODO: Keep the selection and restore.
  @selection.clear()

:restore
  @list.restore()
