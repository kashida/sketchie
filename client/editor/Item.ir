::
  @elem\Element\$
  @selection\item.Selection\$
  @blessItem()

:@blessItem
  @elem.item = @
  @elem.setAttribute('active', '1')
  @elem.dragger = &drag.Move(@elem)
  listen(@elem, 'click', ##)
    evt\Event\$
    if @elem.getAttribute('active') && !evt.ctrlKey
      @selection.select(@elem, evt.shiftKey)
