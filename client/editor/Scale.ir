::<%.Editor
  @selection\item.Selection\$
  @dragger\drag.Scale\ &drag.Scale()
  ^()

:activate
  @dragger.activate(@selection.head)

:dispatch
  evt\Event\$
  if evt.keyCode == DOM_VK_ESCAPE || evt.keyCode == c('A')
    @dragger.deactivate()
    f_editor_modes.popMode()
